<core:FragmentDefinition xmlns="sap.m" xmlns:core="sap.ui.core" xmlns:l="sap.ui.layout">
	<Dialog class="sapUiSizeCompact" title="{i18n>Message}" contentWidth="20%">
		<l:VerticalLayout class="sapUiContentPadding" width="100%">
			<ObjectStatus class="sapUiSmallMarginBottom" text="Please validate due amount." state="Error"/>
			<ObjectStatus class="sapUiSmallMarginBottom" text="Please match the invoice price." state="Error"/>
			<ObjectStatus class="sapUiSmallMarginBottom" text="Please match the PO items." state="Error"/>
		</l:VerticalLayout>
		<!--		<Table inset="false" items="{oInvoiceModel>/aMessage}">
			<columns>
				<Column hAlign="Center" width="6rem">
					<Label text="{i18n>PONumber}" design="Bold"/>
				</Column>
				<Column hAlign="Center" width="5rem">
					<Label text="{i18n>ItemId}" design="Bold"/>
				</Column>
				<Column hAlign="Center" width="14rem">
					<Label text="{i18n>Message}" design="Bold"/>
				</Column>
			</columns>
			<items>
				<ColumnListItem>
					<cells>
						<Text text="{oInvoiceModel>poNumber}"/>
						<Text text="{oInvoiceModel>poItem}"/>
						<Text text="{oInvoiceModel>text}"/>
					</cells>
				</ColumnListItem>
			</items>
		</Table>-->
		<endButton>
			<Button text="{i18n>Close}" press="onCloseMessage"></Button>
		</endButton>
	</Dialog>
</core:FragmentDefinition>